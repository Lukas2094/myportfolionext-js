/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  basePath: '/myportfolionext-js', // Substitua NOME_DO_SEU_REPOSITÓRIO pelo nome do seu repositório no GitHub
  assetPrefix: '/myportfolionext-js/' // Substitua NOME_DO_SEU_REPOSITÓRIO pelo nome do seu repositório no GitHub
}
